import React from 'react';
import Layout from '@theme/Layout';

function Sponsors() {
  return (
    <Layout title="RPM Fusion's sponsors">
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          height: '50vh',
          fontSize: '20px',
        }}>
        <p>
          List of sponsors (NA).
        </p>
      </div>
    </Layout>
  );
}

export default Sponsors;
